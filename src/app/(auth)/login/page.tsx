import Login from "./_components/login";

// Hanya bisa di jalankan di mode server
export const metadata = {
    title: "Jadi Ria | Login",
};

export default function LoginPage() {
    return <Login />;
}
